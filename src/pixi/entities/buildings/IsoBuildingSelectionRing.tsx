import type { ReactNode } from 'react'

export function IsoBuildingSelectionRing({ children }: { children?: ReactNode }): JSX.Element | null {
	return <>{children ?? null}</>
}

export default IsoBuildingSelectionRing

